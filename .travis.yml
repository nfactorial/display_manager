language: cpp

compiler:
 - clang

before_install:
  - sudo add-apt-repository -y ppa:george-edison55/precise-backports
  - sudo apt-get -qq update

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - cmake --version
  - make
  - cd test
  - ./ngen_displaytests